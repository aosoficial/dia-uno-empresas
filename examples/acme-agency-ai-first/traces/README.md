# Traces

Raw traces are for debugging, not curated Company Brain memory.

Store only references, hashes or redacted excerpts unless a human approves more retention.

Receipts should point to traces instead of copying long raw logs into the Company Brain.
